import Sobre from "@/components/Sobre";

export const metadata = { title: "Sobre — Tem no SUS?" };

/** Sem cidade escolhida. Ver `components/Sobre`. */
export default function PaginaSobre() {
  return <Sobre />;
}
